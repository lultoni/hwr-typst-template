# macOS
.DS_Store
**/.DS_Store

# Reference templates (private, for analysis only - not part of public template)
reference-templates/

# HWR internal documents (logo usage restricted, PDF guidelines)
hwr-richtlinien/*

# Typst build artifacts
*.pdf
!template/example.pdf

# Claude Code configuration (local tooling, not part of the template)
.claude/

# User-created project folders (init.sh output — should not end up in the repo)
# Add your project name here to prevent accidentally committing it:
# my-project-name/
